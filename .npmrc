# npm configuration for react-native-ble-mesh

# Use exact versions when saving dependencies
save-exact=false

# Set the default registry
registry=https://registry.npmjs.org/

# Package access (public by default for non-scoped packages)
access=public

# Disable package-lock in published package
package-lock=true

# Git tag version prefix
tag-version-prefix=v

# Commit message format for version bumps
message=chore(release): v%s
