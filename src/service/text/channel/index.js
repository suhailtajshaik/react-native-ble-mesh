'use strict';

/**
 * @fileoverview Channel module exports
 * @module service/text/channel
 */

const Channel = require('./Channel');
const ChannelManager = require('./ChannelManager');

module.exports = {
  Channel,
  ChannelManager
};
